{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "icp",
  "scripts": {
    "deploy": "dfx deploy",
    "deploy:local": "dfx deploy --network local",
    "start": "dfx start --background",
    "stop": "dfx stop",
    "frontend": "cd src/icp_frontend && npm run dev -- --host 0.0.0.0 --port 5000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "workspaces": [
    "src/icp_frontend"
  ],
  "dependencies": {
    "@types/three": "^0.178.1",
    "react-router-dom": "^7.7.0",
    "three": "^0.178.0"
  }
}